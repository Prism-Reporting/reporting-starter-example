OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional override if the reporting MCP server build lives elsewhere.
# Defaults to ../reporting/packages/mcp-server/dist/index.js
REPORTING_MCP_SERVER_PATH=
